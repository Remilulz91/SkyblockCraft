name: Build

on:
  push:
    branches: [main, develop]
    paths-ignore:
      - "**.md"
      - "LICENSE"
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Grant execute permission for gradlew
        run: chmod +x ./gradlew || true

      - name: Download gradle-wrapper.jar if missing
        run: |
          if [ ! -f gradle/wrapper/gradle-wrapper.jar ]; then
            echo "Downloading gradle-wrapper.jar..."
            curl -sSL -o gradle/wrapper/gradle-wrapper.jar https://raw.githubusercontent.com/gradle/gradle/v8.10.0/gradle/wrapper/gradle-wrapper.jar
          fi

      - name: Generate gradlew if missing
        run: |
          if [ ! -f gradlew ]; then
            echo "Bootstrapping gradlew via gradle docker image is not used; instead use the wrapper directly..."
            # If gradlew is missing, the setup-gradle action provides a 'gradle' binary
            gradle wrapper --gradle-version 8.10
          fi

      - name: Build PUBLIC variant
        run: ./gradlew build --stacktrace

      - name: Save public JAR
        run: |
          mkdir -p artifacts
          cp build/libs/*.jar artifacts/

      - name: Build DEBUG variant
        run: ./gradlew clean build -PbuildType=debug --stacktrace

      - name: Save debug JAR
        run: cp build/libs/*.jar artifacts/

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: skyblockcraft-build
          path: artifacts/*.jar
          retention-days: 14
